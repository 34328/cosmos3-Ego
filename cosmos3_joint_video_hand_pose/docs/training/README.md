# 训练文档

- [overfit_v0.0](overfit_v0.0.md)：当前唯一可直接运行的 joint overfit 基线。
- [Two-stage IT2V → Joint](two_stage_it2v_then_joint_v1.md)：后续从纯 IT2V checkpoint 初始化 joint 训练的方案。

实际代码位于 `src/`，运行配置位于 `configs/`，启动脚本位于 `scripts/`；本目录只放文字方案。
