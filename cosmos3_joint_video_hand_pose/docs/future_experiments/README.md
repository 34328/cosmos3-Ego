# Future Experiments

这里保存相对当前 CP1 joint baseline 的研究实验。设计稿必须区分“目标语义”、
“当前最小实现”和“正式同 backend 控制组”，不能把尚未完成的对照写成结论。

- [Video-first WAM modality-causal mask v1](video_first_wam_mask_v1.md)：已实现最小 C/V/A 三角可见性，正在 1200-step 验证；正式 mask-only 结论仍需同 backend full-attention 控制。
